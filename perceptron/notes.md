x

- X represents the input features.
- Each row in X is one data point.
- Each column is a feature (dimension).
- N x 2 matrix, as we can have N points but each point as 2 attributes.

y

- is not vertical axis anymore
- y = label / class
- It tells the model which category the point belongs to.

steps

- create model params
  - need weight and bias
  -
